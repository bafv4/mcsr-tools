{
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    }
  ],
  "buildCommand": "cd ../.. && pnpm build --filter=@mcsr-tools/seedqueue-pack-creator",
  "outputDirectory": "dist"
}
